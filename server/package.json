{
  "name": "lxd-panel",
  "version": "0.0.1",
  "private": true,
  "description": "A secure HTTP REST API for LXD Web Panel",
  "keywords": [],
  "author": "",
  "license": "",
  "main": "app.js",
  "config": {
    "devHost": "0.0.0.0",
    "devPort": 9000
  },
  "scripts": {
    "edit": "swagger project edit .",
    "lint": "eslint --ext .js . && echo No linting errors.",
    "prestart-dev-server": "cd ../ && npm run generate-model-schema && cd server/api && ln -sf ../../models/",
    "start": "sudo ./start-dev-server.sh",
    "start-dev-server": "PORT=$npm_package_config_devPort HOST=$npm_package_config_devHost node ./app",
    "start-prod-server": "node app",
    "test": "swagger project test ."
  },
  "dependencies": {
    "bluebird": "^3.1.1",
    "express": "^4.12.3",
    "jsonwebtoken": "^5.4.1",
    "lxd": "git://github.com/badsyntax/node-lxd.git#develop",
    "swagger-express-mw": "^0.1.0"
  },
  "devDependencies": {
    "asciiparse": "^0.1.1",
    "concurrently": "^1.0.0",
    "eslint": "^1.10.3",
    "should": "^7.1.0",
    "supertest": "^1.0.0",
    "sync-files": "^1.0.3"
  }
}
