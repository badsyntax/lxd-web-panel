# lxd-web-panel server

A secure HTTP REST API for LXD Web Panel

## Starting the server

```
vagrant up
vagrant ssh
cd /vagrant/server
sudo npm start
```

Navigate to http://192.168.50.4:3000

## Starting the Swagger editor

```
npm install -g swagger
swagger project edit .
```


